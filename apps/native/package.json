{
  "name": "mochi-daw-native",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "build:all": "bun run build.ts all",
    "build:framework": "bun run build.ts --framework",
    "build:debug": "bun run build.ts --debug",
    "configure": "bun run build.ts configure",
    "run": "bun run build.ts --run",
    "clean": "bun -e \"import { rmSync, existsSync } from 'node:fs'; const d='build'; if(existsSync(d)){rmSync(d,{recursive:true,force:true}); console.log('removed', d)} else console.log('nothing to clean')\""
  }
}
